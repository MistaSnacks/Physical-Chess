WP2 is done on branch `wp2`. The four lesson types complete in demo, hubs read the live snapshot, and the report is at `agents/reports/WP2.md`.

**Module hubs** at `/learn/movements`, `/learn/music`, `/learn/culture`, and `/learn/graduation` replace the showcase Movements/Music pages. Rails show done/stars/current/locked from the snapshot, with a progress ring and Next-up CTA. Music keeps the instrument field guide and glossary teaser; culture shows the maps, manifestations, and animals; graduation shows the gate. Locked modules stay closed (deep links bounce back with a toast). `/learn/movements` and `/learn/music` still work as redirect targets.

**Lesson player** covers video (60s watch ring or YouTube ended), reading (Listen + sentence highlight, glossary `pt-BR` + “I can say it”), drill (timer, pause, metronome off by default, rating, grown-up confirm), and quiz (hearts, `photoKey`, restart with no XP). Completion writes ledger events, fills stars, and offers Next from `snapshot.nextUp`.

**Verified:** `npm run build` and `npm test` pass. Demo smoke against `dist/client` on port **4402** prints `SMOKE OK` (Maya 295 → 315 after Aú → 370 after a perfect Music quiz). No horizontal overflow at 390 or 1440. Screenshots are in `screenshots/wp2/`.

Content files were not edited. Shared edits: WP2 section in `theme.css`, WP2-owned `Quiz.astro`, and `actions.js` so rapid quiz events are not dropped from the outbox. Commits: `930e3d7`, `41ba32c`. Not pushed.

🚨🚨🚨 Outbox flush was dropping stacked quiz events mid-write; awaiting a leftover-safe flush was the root-cause fix.The static server on port 4402 was stopped after the WP2 smoke test and screenshots finished (exit 143 is the SIGTERM from that shutdown). WP2 itself is already committed and reported.
WP2 exit 0
