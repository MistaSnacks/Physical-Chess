# WP5 — Public site parity

**Owns:** `src/pages/index.astro`, new `src/pages/about/**` (`index`, `educators`, `the-ace`, `abada`, `faq`, `contact`), new `src/pages/graduation/**` (`index`, `what-is-batizado`, `cordas`, `media`), `src/pages/batizado.astro` (keep; link from `/graduation`), new `src/pages/shop.astro`, new `src/pages/api/contact.js`, `src/pages/api/shop/order.js`, `src/directions/quest/Footer.astro`, `src/directions/quest/Nav.astro` (add a "More" menu for public pages; keep the four main links and the session chip as they are), `public/robots.txt`, `public/sitemap.xml` (or `@astrojs/sitemap` if you note it).
**Reads:** `src/content/site.js` (written by WP1; if it is not there yet, create it with the exact export names in `agents/WP1-content.md` §4 and tell WP1 in your report).
**May edit (append):** `theme.css` under `/* === WP5: public === */`.

## Goal
Every item in the Wix member nav (Home · About Us [Educators, the ACE, ABADÁ, F.A.Q., Contact] · Graduation [Batizado 2026, What is it?, Cordas, Batizado Media] · Shop) has a page in the Quest look, using real media from `src/data/media.js` and the verbatim Batizado copy in `src/data/batizado.js`.

## Deliverables
1. **Home** `/`: keep the night-sky hero (Wix mp4 `videoProject1`), age line + contact from `CONTACT`, CTAs: Log in & play (`/login`), Try the demo (`/login?demo=1`, and make `/login?demo=1` auto-open the demo when `IS_DEMO`), Batizado 2026 (`/graduation`). Add a "what you'll learn" strip (four modules as map stops), a "latest from the roda" video, and a footer. Sitemap of public pages in the footer.
2. **About** hub + 5 pages from `site.js`. Educators: portrait cards with `wix(key, 480, 480)` (face-aligned keys already crop to faces), title, bio, `reviewNeeded` shows a small "copy pending ACE review" note only when `import.meta.env.DEV`. Contact: phone/email from `CONTACT`, a form (name, email, message) posting to `/api/contact`, which inserts into the Wix `contactForm` collection using the admin token (`adminToken()` in `src/lib/server/session.js`) and returns `{ ok }`; when `WIX_CLIENT_SECRET` is missing, the route returns `{ ok: true, demo: true }` and the page says "Demo: nothing was sent".
3. **Graduation** hub linking Batizado 2026 (`/batizado`), What is it? (verbatim `INTRO` + `MOMENTS` from `batizado.js`), Cordas (ladder from `CORDAS` with swatches and `CORDAS_PAGE` copy), Media (gallery grouped by year using the 2016–2022 captions on the public Wix "Batizado Media" page: 2016 Feiticeira e Gatinha Jogando, Prof. Coco e Dama, Kids playing with Parents, Prof. Coco e Tubarão, Troca de Cordas, Toro e Mindinha, Adults receiving their first rank, Volta Do Mundo; 2017 Mes. Goma com Criança, Getting their new cordas, Prof. Yara com Gatinha, Cords Table 2017, Jongo HSA 2017, Aquecimento, Final Celebration; 2018 Cords Table 2018, Pronto para Evento, Instructor Batizando a Criança, Volta do Mundo, Final Celebration 2018; 2019 ABADA Capoeira Brooklyn Batizado 2019, Pastor e Ferdinand, Group Foto 2019, Xampu e Gasparzinho, Troca de Cordas, Samba no Batizado; 2020 Crianças jogando na roda, Isabella e Emanuel; 2022 Cords Table 2022, Curso para crianças com Professor Coco, Wrapping up the event, Pastor com Perigo e Ciborg, Tubarão e Guindaste — match each caption to the closest key in `media.js` and skip captions with no photo) plus YouTube 2024/2025/2026.
4. **Shop** `/shop`: uniform top order form per `site.js` SHOP; posts to `/api/shop/order` which inserts into the Wix `contactForm3` ("Uniform Tops") collection with the same field keys it already has (`parentsName`, `childsNameShirtPan`, `email`, `schoolProgramAndChild`, `contact`, `submissionTime`) via the admin token; demo behavior as above. Confirmation state on the page.
5. **Nav**: a "More" dropdown (About, Graduation, Shop, Privacy) that works with keyboard and at 390 (turns into a sheet). Footer: contact, age line, links, "Video: SoChlo Productions" credit where videos appear.
6. `public/robots.txt`, a sitemap of public routes, `<meta name="description">` per page via the `Base` `description` prop, Open Graph image = `wix('heroWorkshop', 1200, 630)`.

## Done when
Every Wix nav item resolves to a Quest page; forms hit their routes; Lighthouse performance ≥ 85 on `/` (Wix crops ≤ 1600 px wide, lazy images); screenshots in `screenshots/wp5/`; report `agents/reports/WP5.md`.
